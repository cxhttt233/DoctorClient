# Apache Hop Web multi-session hotfix workspace

Temporary build/test workspace for the Hop Web session-isolation hotfix based on apache/hop main.

This repository is used only as a CI/build harness. The upstream source remains apache/hop.
